spring.application.name=backend

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/bidmart}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:bidmart}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:bidmart}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.sql.init.mode=always
