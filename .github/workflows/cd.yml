name: CD (dummy)

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main' }}
    runs-on: ubuntu-latest
    steps:
      - name: Dummy deploy
        run: |
          echo "CI passed; running dummy deployment..."
          echo "Commit: ${{ github.event.workflow_run.head_sha }}"